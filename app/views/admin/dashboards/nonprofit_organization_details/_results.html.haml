- if @results.length == 0
  There is nothig to show for the specified time range
- else
  %table{:cellpadding => 0, :cellspacing => 0, :border => 0, :id => 'table2', :class => 'sortable'}
    %thead
      %tr
        %th
          %h3 Organization Name
        %th
          %h3 Amount Earned
        %th
          %h3 Total Survey Responses
        %th
          %h3 Percentage Changed
    %tbody
      = nonprofit_orgs_results({:records => @results, :ranges => @segmented_data})