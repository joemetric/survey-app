%table{:cellpadding => 0, :cellspacing => 0, :border => 0, :id => 'table2', :class => 'sortable'}
  %thead
    %tr
      %th
        %h3= @segmented_data[:header]
      %th
        %h3= @header.humanize
  %tbody
    = survey_distribution_results({:records => @results, :ranges => @segmented_data[:ranges], :class => @results_class})