%table{:cellpadding => 0, :cellspacing => 0, :border => 0, :id => 'table2', :class => 'sortable'}
  %thead
    %tr
      %th
        %h3= @segmented_data[:header]
      %th
        %h3 # Completed Surveys
  %tbody
    = table_rows_for @surveys, @segmented_data[:ranges]